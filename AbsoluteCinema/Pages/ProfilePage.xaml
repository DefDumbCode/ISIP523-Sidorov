<Page x:Class="AbsoluteCinema.Pages.ProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AbsoluteCinema.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProfilePage">

    <Grid>
        <StackPanel HorizontalAlignment="Center" Width="325">
            <Label Content="ПРОФИЛЬ" HorizontalAlignment="Center" FontSize="36" Margin="10"/>
            <TextBlock Text="{Binding AccountLogin, StringFormat=Логин: {0}}" TextAlignment="Center"  FontSize="24" TextWrapping="Wrap" Margin="0 75"/>
            <TextBlock Text="Билеты" TextAlignment="Center"  FontSize="24"/>
            <ListBox x:Name="TicketsLB" ItemsSource="{Binding Ticket}" d:ItemsSource="{d:SampleData ItemCount=5}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled" SelectionChanged="ListBox_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="150">
                            <TextBlock Text="{Binding Session.Films.FilmName}"/>
                            <TextBlock Text="{Binding Session.Date}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Stretch"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </StackPanel>
        <Button x:Name="BackBtn" Content="Назад"  Click="BackBtn_Click" 
                 HorizontalAlignment="Left" VerticalAlignment="Top" Width="125" Height="75" Margin="25 20"/>
    </Grid>
</Page>
