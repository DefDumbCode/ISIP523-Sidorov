<Page x:Class="AbsoluteCinema.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AbsoluteCinema.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="80*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition Width="60*"/>
            <ColumnDefinition Width="20*"/>
        </Grid.ColumnDefinitions>

        <ComboBox x:Name="SortByCB" VerticalContentAlignment="Center"
                  Height="35" Width="125" Margin="5" SelectionChanged="SortByCB_SelectionChanged"/>

         <TextBox x:Name="SearchTB" Grid.Column="1" VerticalContentAlignment="Center"
         Height="30" Width="200" FontSize="18" TextChanged="SearchTB_TextChanged"/>

        <Button x:Name="LogInBtn" Grid.Row="0" Grid.Column="2" Height="55" Width="150" Margin="5"/>

        <ListBox x:Name="FilmsLB" SelectionChanged="FilmsLB_SelectionChanged"
                 Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="200">
                        <Image Source="{Binding ImagePath}" Width="150" Height="250"/>
                        <TextBlock Text="{Binding FilmName}" FontSize="16" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding FilmRate, StringFormat=Рейтинг: {0}}" FontSize="14"/>
                        <TextBlock Text="{Binding AgeRates.AgeRateName}" FontSize="14"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel HorizontalAlignment="Stretch"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</Page>
